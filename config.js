// config.js — Edit these values before running the bot
module.exports = {
  // Your bot token from https://discord.com/developers/applications
  token: process.env.DISCORD_TOKEN || 'YOUR_BOT_TOKEN_HERE',

  // Command prefix (default: !fh)
  prefix: '!fh',
};
